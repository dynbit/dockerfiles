version: 2
jobs:
  build:
    working_directory: ~/build
    docker:
      - image: ubuntu:18.04
    steps:
      - checkout
      - setup_remote_docker:
          reusable: true
          exclusive: true
      - run:
          name: Build image
          command: make golang-dep-image
      - deploy:
          name: Upload image
          command: make golang-dep-upload

